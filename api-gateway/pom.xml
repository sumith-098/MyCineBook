<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.5</version>
        <relativePath/>
    </parent>

    <groupId>com.cinebook</groupId>
    <artifactId>api-gateway</artifactId>
    <version>1.0.0</version>
    <name>api-gateway</name>
    <description>CineBook API Gateway - single entry point for the React frontend, routes to all services, validates JWT at the edge</description>

    <properties>
        <java.version>21</java.version>
        <!-- Pinned to 2023.0.3 on purpose — NOT the newest 2023.0.x patch. Spring Cloud
             Gateway 4.1.6 (pulled in by 2023.0.5) calls HttpHeaders.headerSet(), a method
             that only exists in Spring Framework 6.2+. Spring Boot 3.3.5 ships Framework
             6.1.14, which doesn't have it — that's a NoSuchMethodError at runtime, not a
             build-time error, so Maven won't warn you. See spring-cloud/spring-cloud-gateway#3596. -->
        <spring-cloud.version>2023.0.3</spring-cloud.version>
        <!-- Belt-and-suspenders: pin the gateway artifact itself so a future BOM bump can't
             silently reintroduce the same regression. -->
        <spring-cloud-gateway.version>4.1.5</spring-cloud-gateway.version>
        <jjwt.version>0.12.6</jjwt.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Explicit override — wins over whatever spring-cloud-dependencies would pick,
                 regardless of which patch of the BOM resolves here later. -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-gateway-server</artifactId>
                <version>${spring-cloud-gateway.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-gateway</artifactId>
        </dependency>

        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- No dotenv library — .env loading is handled by Spring Boot's own
             `spring.config.import: optional:file:.env[.properties]` in application.yml. -->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>